<%- include('layout/header') %>

<div class="admin_view_req_archi"><div class="canvas_div_pdf">
    <br/>
    <center><h2>Client Requirements for the Architectures</h2></center>
    <div class="row my-4">
        <div class="col-lg-26">
            <% if (message) { %> 
                <div class="alert alert-dismissible fade show alert-<%= message.type %>" role="alert">
                    <button class="btn-close" type="button" data-bs-dismiss="alert"
                    arial-label="Close"></button>
                    <strong><%= message.message %></strong>
                </div>
            <% } %>

            <div class="table-responsive">

                <% if (data != '') { %>
                    <table  class="table table-striped text-center">
                        <thead>
                            <tr class="table-dark tblheadarchi">
                                <th>Architecture</th>
                                <th>Client Name</th>
                                <th>Email</th>
                                <th>Building Type</th>
                                <th>No of floors</th>
                                <th>Estimated Budget</th>
                                <th>Location</th>
                                <th>Plan of the Land</th>
                                <th>Bed Rooms</th>
                                <th>Bathrooms</th>
                                <th>Parking Area</th>
                                <th>Other Requirements</th>
                                
                            </tr>
                        </thead>
                        <tbody class="tblbodyarchi">
                            <% data.forEach((row,index)=> { %>
                            <tr class="align-middle">
                                <td><%= row.Architecture %></td>
                                <td><%= row.Client_Name %></td>
                                <td><%= row.Email %></td>
                                <td><%= row.Builging_Type %></td>
                                <td><%= row. No_of_floors %></td>
                                <td><%= row.Estimated_Budget %></td>
                                <td><%= row.Location %></td>
                                <td><img src="<%= row.Plan_of_the_Land %>" width="100" class="img-proj1" mt-1 style=" border: 1px solid rgb(0, 0, 0);" /></td>
                                <td><%= row.Bed_Rooms %></td>
                                <td><%= row.Bathrooms %></td>
                                <td><%= row.Parking_Area %></td>
                                <td><%= row.Other_Requirements %></td>
                 
                            </tr>
                             <% }) %>
                        </tbody>
                    </table>
                <% }  else { %>
                    <h1 class="text-center text-secondary mt-5">No employee found in the database .!</h1>
                <% } %>

                
            </div>
        </div>
    </div>
</div>

<br/><br/>
<button class="btn btn-outline-info btn-lg" onclick="getPDF();">
    <i class="fa-solid fa-download">
    </i> Download
    </button>
</div>
<%- include('layout/footer') %>